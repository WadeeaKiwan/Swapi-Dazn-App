import React from "react";
import PropTypes from "prop-types";

import { withStyles } from "@material-ui/core";

const styles = (theme) => ({
  ...theme.styles
});

const ActorList = ({ classes }) => {
  return <div></div>;
};

ActorList.propTypes = {};

export default withStyles(styles)(ActorList);
